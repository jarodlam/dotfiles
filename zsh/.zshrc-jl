# Layered on top of system config.
# Add to top of system ~/.zshrc:
#   source ~/.zshrc-jl

# Enable zsh completion system
autoload -Uz compinit
autoload -Uz bashcompinit
compinit
bashcompinit

# zsh vi mode
bindkey -v

# zsh history settings
HISTFILE=~/.zsh_history
HISTFILESIZE=10000000
HISTSIZE=10000000
SAVEHIST=500000
setopt INC_APPEND_HISTORY
setopt SHARE_HISTORY
setopt APPENDHISTORY

# zsh autocomplete dot files
setopt GLOBDOTS

# zsh syntax highlighting plugin
source ~/.config/zsh/fast-syntax-highlighting/fast-syntax-highlighting.plugin.zsh

# zsh vi mode plugin (deferred init clobbers keybindings, so use
# zvm_after_init hook to restore fzf bindings after it initializes)
function zvm_after_init() {
  source <(fzf --zsh)
}
source ~/.config/zsh/zsh-vi-mode/zsh-vi-mode.plugin.zsh
ZVM_SYSTEM_CLIPBOARD_ENABLED=true
ZVM_LINE_INIT_MODE=$ZVM_MODE_INSERT

# default editor
export VISUAL=nvim
export EDITOR=nvim

# zsh instant ESC key for vi mode
# https://github.com/starship/starship/discussions/7104#discussioncomment-15344825
export KEYTIMEOUT=1

# starship (guard against re-source: starship wraps zle-keymap-select,
# and re-wrapping causes infinite recursion)
if [[ -z "${_starship_initialized:-}" ]]; then
  eval "$(starship init zsh)"
  _starship_initialized=1
fi

# yazi
function y() {
  local tmp="$(mktemp -t "yazi-cwd.XXXXXX")" cwd
  yazi "$@" --cwd-file="$tmp"
  IFS= read -r -d '' cwd < "$tmp"
  [ -n "$cwd" ] && [ "$cwd" != "$PWD" ] && builtin cd -- "$cwd"
  rm -f -- "$tmp"
}

# direnv
eval "$(direnv hook zsh)"

# aliases
alias nd='(){ nix develop .#$1 -vv --command zsh }'
alias nr='ulimit -v 10485760 && nix repl'
alias lg='lazygit'
